(function(){
'use strict';
const V2=window.zukaitV2=window.zukaitV2||{}, p=V2.spareParts=V2.spareParts||{};
const STATUS=['LISTED','ENQUIRY','QUOTED','ORDERED','RECEIVED','DENTER_CHECKED','SUPERVISOR_CONFIRMED','FITTED','RETURNED','UNAVAILABLE','CUSTOMER_SETTLEMENT'];
const transitions={LISTED:['ENQUIRY','UNAVAILABLE'],ENQUIRY:['QUOTED','UNAVAILABLE'],QUOTED:['ORDERED','ENQUIRY','UNAVAILABLE'],ORDERED:['RECEIVED','RETURNED','UNAVAILABLE'],RECEIVED:['DENTER_CHECKED','SUPERVISOR_CONFIRMED','RETURNED'],DENTER_CHECKED:['SUPERVISOR_CONFIRMED','RETURNED'],SUPERVISOR_CONFIRMED:['FITTED','RETURNED'],UNAVAILABLE:['CUSTOMER_SETTLEMENT'],RETURNED:['ENQUIRY','ORDERED','UNAVAILABLE']};
function canSeePrice(role){return ['Manager','Supervisor','Purchaser'].includes(String(role||''))}
function allowed(from,to){return (transitions[String(from||'')]||[]).includes(String(to||''))}
function canAct(role,from,to){role=String(role||'');if(role==='Manager')return STATUS.includes(String(to||''));if(!allowed(from,to))return false;if(['ENQUIRY','QUOTED','ORDERED','RECEIVED'].includes(to))return role==='Purchaser';if(to==='DENTER_CHECKED')return role==='Denter';if(to==='SUPERVISOR_CONFIRMED')return role==='Supervisor';if(to==='FITTED')return ['Denter','Supervisor'].includes(role);if(to==='RETURNED')return ['Supervisor','Purchaser'].includes(role);if(['UNAVAILABLE','CUSTOMER_SETTLEMENT'].includes(to))return ['Supervisor','Purchaser'].includes(role);return role==='Supervisor'}
function sanitize(item,role){const out=Object.assign({},item);if(!canSeePrice(role)){['price','finalPriceOMR','purchaseAmount','quoteAmount','billAmount','supplierCost','quotation','shops','purchaseCap'].forEach(k=>delete out[k])}return out}
function transition(item,to,ctx={}){const from=String(item?.status||'LISTED');if(!canAct(ctx.role,from,to))return{ok:false,reason:'FORBIDDEN_OR_INVALID_TRANSITION',from,to};const now=ctx.serverTime||new Date().toISOString(),next=Object.assign({},item,{status:to,updatedAt:now,updatedBy:ctx.actorId||null});if(to==='UNAVAILABLE')next.cashSettlementRequired=true;if(to==='CUSTOMER_SETTLEMENT')next.cashSettlementRequired=false;if(to==='RETURNED')next.returnReason=String(ctx.reason||'').trim();if(to==='RECEIVED'){next.receivedAt=now;next.receivedBy=ctx.actorId||null;next.awaitingSupervisorCheck=true}if(to==='DENTER_CHECKED'){next.denterCheckedAt=now;next.denterCheckedBy=ctx.actorId||null}if(to==='SUPERVISOR_CONFIRMED'){next.confirmedAt=now;next.confirmedBy=ctx.actorId||null;next.awaitingSupervisorCheck=false}return{ok:true,item:next,audit:{type:'SPARE_PART_STATUS_CHANGED',entityId:String(item?.id||''),from,to,actorId:ctx.actorId||null,deviceId:ctx.deviceId||null,reason:ctx.reason||null,at:now}}}
function flag(item,kind,ctx={}){if(!['URGENT','DEFECT'].includes(kind)||!['Denter','Supervisor','Manager'].includes(String(ctx.role||'')))return{ok:false,reason:'FORBIDDEN_FLAG'};const now=ctx.serverTime||new Date().toISOString();return{ok:true,item:Object.assign({},item,{[kind==='URGENT'?'urgent':'defect']:true,flaggedAt:now,flaggedBy:ctx.actorId||null}),notification:{type:'SPARE_PART_'+kind,jobCard:item?.jobCard||null,partId:item?.id||null,targetRole:'Supervisor',dedupeKey:['spare',kind,item?.id||'',item?.status||''].join(':')}}}
Object.assign(p,{STATUS,canSeePrice,allowed,canAct,sanitize,transition,flag});
})();