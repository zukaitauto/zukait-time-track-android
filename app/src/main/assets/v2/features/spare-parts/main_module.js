(function(){'use strict';
const KEY='zukait_v2_spare_parts_lists_v1';
function read(){try{return JSON.parse(localStorage.getItem(KEY)||'[]')}catch(_){return[]}}
function write(v){localStorage.setItem(KEY,JSON.stringify(v));return v}
function normalizeJobCard(v){return String(v||'').trim().toUpperCase()}
function existingOpen(rows,jobCard){return rows.find(r=>normalizeJobCard(r.jobCard)===jobCard&&r.status!=='CLOSED')}
function currentUser(){try{if(typeof me!=='undefined'&&me)return me}catch(_){}return window.me||window.currentUser||null}
function role(){return String(currentUser()?.role||'')}
function canOpen(){return ['Manager','Supervisor','Purchaser','Denter'].includes(role())}
function canManage(){return ['Manager','Supervisor'].includes(role())}\nfunction canPurchase(){return ['Manager','Purchaser'].includes(role())}
function denterView(){return role()==='Denter'}
function statusColour(status){const s=String(status||'LISTED');return ({LISTED:'#64748b',ENQUIRY:'#2563eb',QUOTED:'#7c3aed',ORDERED:'#d97706',RECEIVED:'#0891b2',DENTER_CHECKED:'#0f766e',SUPERVISOR_CONFIRMED:'#16a34a',FITTED:'#15803d',RETURNED:'#dc2626',UNAVAILABLE:'#b91c1c',CUSTOMER_SETTLEMENT:'#9333ea'})[s]||'#64748b'}
function job(no){try{if(typeof window.jd==='function'){const j=window.jd(no);if(j)return j}if(typeof window.job==='function')return window.job(no);return null}catch(_){return null}}
function assignedToJob(no,uid){try{if(typeof window.assignment==='function'&&window.assignment(no,uid))return true;const a=window.state?.assign||[];return a.some(x=>x&&String(x.job)===String(no)&&String(x.emp)===String(uid)&&!x.cancelled)}catch(_){return false}}
function listsForJobCard(jobCard){const jc=normalizeJobCard(jobCard);return read().filter(r=>normalizeJobCard(r.jobCard)===jc&&r.status!=='CLOSED')}
function partsSummary(jobCard){const lists=listsForJobCard(jobCard),items=lists.flatMap(r=>Array.isArray(r.items)?r.items:[]);return {exists:lists.length>0,count:items.length,listNo:lists[0]?.listNo||null,statuses:items.map(x=>x.status||'LISTED')}}
function openForJobCard(jobCard){const jc=normalizeJobCard(jobCard),lists=listsForJobCard(jc);if(!lists.length)return {ok:false,reason:'NO_PARTS_LIST'};openList(lists[0].listNo);return {ok:true,list:lists[0]}}

async function commitEvent(event){const q=window.zukaitV2?.queue;if(!window.zukaitCloud?.v2CommitEvent||!navigator.onLine){try{q?.enqueue?.(event)}catch(_){}return {ok:false,reason:'V2_TRANSPORT_UNAVAILABLE',queued:true}}try{const r=await window.zukaitCloud.v2CommitEvent(event);return {ok:true,result:r}}catch(e){const reason=String(e?.code||e?.message||e);if(['NETWORK','TIMEOUT','NO_SESSION','Failed to fetch'].some(x=>reason.includes(x))){try{q?.enqueue?.(event)}catch(_){}return {ok:false,reason,queued:true}}return {ok:false,reason}}}
function create(jobCard){return {ok:false,reason:'SERVER_ALLOCATION_REQUIRED',jobCard:normalizeJobCard(jobCard)}}
function addItem(listNo,item){if(!canManage())return {ok:false,reason:'FORBIDDEN'};const rows=read(),r=rows.find(x=>x.listNo===listNo);if(!r)return {ok:false,reason:'LIST_NOT_FOUND'};if(r.status==='CLOSED')return {ok:false,reason:'LIST_CLOSED'};const name=String(item?.name||item?.part||'').trim();if(!name)return {ok:false,reason:'PART_REQUIRED'};const partNo=String(item?.partNo||'').trim().toUpperCase();const duplicate=r.items.find(x=>String(x.name||'').trim().toLowerCase()===name.toLowerCase()&&String(x.partNo||'').trim().toUpperCase()===partNo&&x.status!=='RETURNED');if(duplicate)return {ok:false,reason:'DUPLICATE_PART',item:duplicate};r.items.push({id:'SP-'+Date.now()+'-'+(r.items.length+1),name,partNo,qty:validQty(item?.qty)||1,status:'LISTED',createdAt:new Date().toISOString()});r.updatedAt=new Date().toISOString();write(rows);return {ok:true,list:r}}
function validQty(v){const n=Number(v);return Number.isFinite(n)&&n>0&&n<=9999?n:null}
function validMoney(v){if(v==null||v==='')return null;const n=Number(v);return Number.isFinite(n)&&n>=0&&n<=1000000?Math.round(n*1000)/1000:null}
function updateCommercial(listNo,itemId,patch={}){const u=currentUser()||{};if(!canPurchase())return {ok:false,reason:'PRICE_PERMISSION_REQUIRED'};const rows=read(),list=rows.find(x=>x.listNo===listNo);if(!list)return {ok:false,reason:'LIST_NOT_FOUND'};const item=list.items.find(x=>x.id===itemId);if(!item)return {ok:false,reason:'PART_NOT_FOUND'};if(['FITTED','CUSTOMER_SETTLEMENT'].includes(item.status))return {ok:false,reason:'PART_FINALIZED'};if(Object.prototype.hasOwnProperty.call(patch,'qty')){const q=validQty(patch.qty);if(q==null)return {ok:false,reason:'INVALID_QUANTITY'};item.qty=q}for(const k of ['price','quoteAmount','purchaseAmount','billAmount','supplierCost'])if(Object.prototype.hasOwnProperty.call(patch,k)){const n=validMoney(patch[k]);if(n==null&&patch[k]!==''&&patch[k]!=null)return {ok:false,reason:'INVALID_AMOUNT'};if(n==null)delete item[k];else item[k]=n}if(Object.prototype.hasOwnProperty.call(patch,'supplier'))item.supplier=String(patch.supplier||'').trim();item.updatedAt=new Date().toISOString();item.updatedBy=u.id||null;list.updatedAt=item.updatedAt;write(rows);return {ok:true,item}}
function spareBtn(kind='neutral'){const map={primary:'#2563eb',info:'#0ea5e9',wait:'#d97706',success:'#16a34a',danger:'#dc2626',edit:'#7c3aed',neutral:'#64748b'};return 'min-height:44px;border:0;border-radius:11px;background:'+map[kind]+';color:#fff;font-weight:900;padding:10px 12px;box-shadow:0 4px 12px #0f172a18'}
function navButtons(back){return '<div class="v2-sp-nav" style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:12px 0"><button type="button" onclick="'+(back||'zukaitV2.sparePartsMain.open()')+'" style="'+spareBtn('primary')+'">← Back</button><button type="button" onclick="closeModal()" style="'+spareBtn('danger')+'">✕ Close</button></div>'}
function withNav(html,back){return navButtons(back)+html+navButtons(back)}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function reportAmount(item){for(const k of ['purchaseAmount','supplierCost','billAmount','price']){const n=Number(item?.[k]);if(Number.isFinite(n)&&n>=0)return n}return 0}
function reportActivityAt(item,list){const dates=[item?.updatedAt,item?.fittedAt,item?.returnedAt,item?.confirmedAt,item?.supervisorVerifiedAt,item?.receivedAt,item?.createdAt,list?.updatedAt,list?.createdAt].filter(Boolean).map(String).filter(x=>!Number.isNaN(Date.parse(x))).sort((a,b)=>Date.parse(b)-Date.parse(a));return dates[0]||''}
function reportRows(){return read().flatMap(list=>(Array.isArray(list.items)?list.items:[]).map(item=>({listNo:list.listNo,jobCard:list.jobCard,vehicle:[list.vehicle,list.model,list.year,list.registration].filter(Boolean).join(' · '),createdAt:item.createdAt||list.createdAt||'',activityAt:reportActivityAt(item,list),name:item.name||'',partNo:item.partNo||'',qty:Number(item.qty)||0,status:item.status||'LISTED',supplier:item.supplier||'',amount:reportAmount(item)})))}
function normalizeReportRow(r={}){const qty=Number(r.qty??r.quantity??0),amount=Number(r.amount??r.purchaseAmount??r.purchase_amount??r.supplierCost??r.supplier_cost??r.billAmount??r.bill_amount??r.price??0);return {listNo:String(r.listNo??r.list_no??r.partsListNo??r.parts_list_no??''),jobCard:String(r.jobCard??r.job_card??r.jobCardNo??r.job_card_no??''),vehicle:String(r.vehicle??r.vehicle_name??[r.make,r.model,r.year,r.registration??r.reg].filter(Boolean).join(' · ')??''),createdAt:String(r.createdAt??r.created_at??''),activityAt:String(r.activityAt??r.activity_at??r.updatedAt??r.updated_at??r.createdAt??r.created_at??''),name:String(r.name??r.partName??r.part_name??r.part??''),partNo:String(r.partNo??r.part_no??''),qty:Number.isFinite(qty)&&qty>=0?qty:0,status:String(r.status??'LISTED').toUpperCase(),supplier:String(r.supplier??r.supplier_name??''),amount:Number.isFinite(amount)&&amount>=0?Math.round(amount*1000)/1000:0}}
function hydrateFromServerRows(serverRows){const local=read(),byList=new Map(local.map(x=>[String(x.listNo||''),x]));for(const raw of serverRows||[]){const r=normalizeReportRow(raw);if(!r.listNo)continue;let list=byList.get(r.listNo);if(!list){list={listNo:r.listNo,jobCard:r.jobCard,vehicle:r.vehicle,items:[],createdAt:r.createdAt||r.activityAt||new Date().toISOString(),serverHydrated:true};local.unshift(list);byList.set(r.listNo,list)}if(r.jobCard&&!list.jobCard)list.jobCard=r.jobCard;if(r.vehicle&&!list.vehicle)list.vehicle=r.vehicle;if(r.name){const key=String(raw.partId||raw.part_id||r.partNo||r.name);let item=(list.items||[]).find(x=>String(x.id||x.partNo||x.name)===key);if(!item){item={id:key,name:r.name,partNo:r.partNo,qty:r.qty,status:r.status,supplier:r.supplier,purchaseAmount:r.amount,updatedAt:r.activityAt};list.items.push(item)}else if(Date.parse(r.activityAt||0)>=Date.parse(item.updatedAt||0)){Object.assign(item,{name:r.name,partNo:r.partNo||item.partNo,qty:r.qty,status:r.status,supplier:r.supplier,purchaseAmount:r.amount,updatedAt:r.activityAt})}}}write(local);return local}
async function hydrateAuthoritativeLists(){if(!navigator.onLine||!window.zukaitV2?.reports?.page)return read();try{let cursor=null,rows=[],pages=0;do{const r=await window.zukaitV2.reports.page('SPARE_PARTS',{cursor,limit:500,filters:{}});if(!Array.isArray(r?.rows)||r.source==='server-required')return read();rows.push(...r.rows);cursor=r.nextCursor??null;pages++}while(cursor&&pages<20);return hydrateFromServerRows(rows)}catch(_){return read()}}
async function loadAuthoritativeManagerReport(){if(role()!=='Manager')return {rows:[],source:'forbidden'};const svc=window.zukaitV2?.reports;if(!navigator.onLine||!svc?.page)return {rows:reportRows(),source:'local-fallback'};try{let cursor=null,rows=[],source='server',pages=0;do{const r=await svc.page('SPARE_PARTS',{cursor,limit:500,filters:{}});if(!Array.isArray(r?.rows)||r.source==='server-required')return {rows:reportRows(),source:'local-fallback'};rows.push(...r.rows.map(normalizeReportRow));source=r.source||source;cursor=r.nextCursor??null;pages++}while(cursor&&pages<20);return {rows,source:cursor?'server-capped-10000':source}}catch(_){return {rows:reportRows(),source:'local-fallback'}}}
let managerReportSourceRows=null,managerReportSource='local-fallback';
async function openManagerReport(){if(role()!=='Manager'){alert('Manager access required.');return}const loaded=await loadAuthoritativeManagerReport();managerReportSourceRows=loaded.rows;managerReportSource=loaded.source;window.openModal(withNav('<h3>📊 Spare Parts Report</h3><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px"><label>From<input id="v2SpReportFrom" type="date"></label><label>To<input id="v2SpReportTo" type="date"></label></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px"><input id="v2SpReportSearch" placeholder="JC / PL / vehicle / part"><select id="v2SpReportStatus"><option value="">All statuses</option>'+((window.zukaitV2?.spareParts?.STATUS)||[]).map(s=>'<option>'+esc(s)+'</option>').join('')+'</select></div><button class="blue" onclick="zukaitV2.sparePartsMain.renderManagerReport()" style="margin-top:8px">Apply</button><div style="display:flex;gap:6px;flex-wrap:wrap;margin-top:8px"><button class="secondary" onclick="zukaitV2.sparePartsMain.printManagerReport()">Print / PDF</button><button class="secondary" onclick="zukaitV2.sparePartsMain.whatsAppManagerReport()">WhatsApp</button><button class="secondary" onclick="zukaitV2.sparePartsMain.shareManagerReport()">Share</button><button class="secondary" onclick="zukaitV2.sparePartsMain.open()">Back</button></div><div id="v2SpReportBody"></div>');renderManagerReport()}
function managerReportFilters(){return {from:document.getElementById('v2SpReportFrom')?.value||'',to:document.getElementById('v2SpReportTo')?.value||'',q:String(document.getElementById('v2SpReportSearch')?.value||'').trim().toLowerCase(),status:document.getElementById('v2SpReportStatus')?.value||''}}
function filteredReportRows(){const f=managerReportFilters();const base=Array.isArray(managerReportSourceRows)?managerReportSourceRows:reportRows();return base.filter(r=>{const day=String(r.activityAt||r.createdAt||'').slice(0,10);if(f.from&&(!day||day<f.from))return false;if(f.to&&(!day||day>f.to))return false;if(f.status&&r.status!==f.status)return false;if(f.q&&!([r.listNo,r.jobCard,r.vehicle,r.name,r.partNo,r.supplier].join(' ').toLowerCase().includes(f.q)))return false;return true})}
function managerReportText(){const rows=filteredReportRows(),f=managerReportFilters();const range=[f.from?('From '+f.from):'',f.to?('To '+f.to):'',f.status?('Status '+f.status):'',f.q?('Search '+f.q):''].filter(Boolean).join(' | ');const lines=['ZUKAIT INTERNATIONAL LLC','SPARE PARTS REPORT',range,''];rows.forEach(r=>lines.push([r.listNo,'JC '+r.jobCard,r.vehicle,r.name,'Qty '+r.qty,r.status,r.supplier,r.amount.toFixed(3)+' OMR'].filter(Boolean).join(' | ')));return lines.join('\n')}
function printManagerReport(){if(role()!=='Manager')return;const body=document.getElementById('v2SpReportBody');if(!body)return;const w=window.open('','_blank');if(!w){alert('Print window blocked.');return}w.document.write('<!doctype html><html><head><title>Spare Parts Report</title><style>body{font-family:Arial;padding:20px;color:#111}table{width:100%;border-collapse:collapse}th,td{border:1px solid #bbb;padding:6px;text-align:left}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.notice{border:1px solid #ddd;padding:8px}.stat{font-size:20px;font-weight:bold}.pill{display:inline-block;padding:4px 7px;border:1px solid #bbb;margin:2px}.small{font-size:11px}</style></head><body><h2>Zukait International LLC</h2><h3>Spare Parts Report</h3>'+body.innerHTML+'</body></html>');w.document.close();w.focus();w.print()}
function whatsAppReportText(){const rows=filteredReportRows(),f=managerReportFilters(),max=40,shown=rows.slice(0,max),qty=rows.reduce((a,r)=>a+r.qty,0),amount=Math.round(rows.reduce((a,r)=>a+r.amount,0)*1000)/1000;const range=[f.from?('From '+f.from):'',f.to?('To '+f.to):'',f.status?('Status '+f.status):'',f.q?('Search '+f.q):''].filter(Boolean).join(' | ');const lines=['ZUKAIT INTERNATIONAL LLC','SPARE PARTS REPORT',range,'Parts Lists: '+new Set(rows.map(r=>r.listNo)).size+' | Part Lines: '+rows.length+' | Total Qty: '+qty+' | Recorded Amount: '+amount.toFixed(3)+' OMR',''];shown.forEach(r=>lines.push([r.listNo,'JC '+r.jobCard,r.name,'Qty '+r.qty,r.status].filter(Boolean).join(' | ')));if(rows.length>max)lines.push('... '+(rows.length-max)+' more part line(s). Use Print / PDF for the complete report.');return lines.join('\n')}
function whatsAppManagerReport(){if(role()!=='Manager')return;const text=whatsAppReportText(),url='https://wa.me/?text='+encodeURIComponent(text);try{const w=window.open(url,'_blank');if(w)return}catch(_){}window.location.href=url}
async function shareManagerReport(){if(role()!=='Manager')return;const text=managerReportText();try{if(navigator.share){await navigator.share({title:'Spare Parts Report',text});return}}catch(e){if(e?.name==='AbortError')return}try{await navigator.clipboard.writeText(text);alert('Spare Parts report copied. You can share it from this device.')}catch(_){alert('Share is not available on this device. Use Print / PDF.')}}
function renderManagerReport(){if(role()!=='Manager')return;const rows=filteredReportRows();const qty=rows.reduce((a,r)=>a+r.qty,0),cost=Math.round(rows.reduce((a,r)=>a+r.amount,0)*1000)/1000,fittedQty=rows.filter(r=>r.status==='FITTED').reduce((a,r)=>a+r.qty,0),returnedQty=rows.filter(r=>r.status==='RETURNED').reduce((a,r)=>a+r.qty,0),unavailableQty=rows.filter(r=>r.status==='UNAVAILABLE').reduce((a,r)=>a+r.qty,0),statuses={};rows.forEach(r=>statuses[r.status]=(statuses[r.status]||0)+1);const sourceNote='<div class="notice"><b>Data source:</b> '+esc(managerReportSource==='local-fallback'?'Local fallback — server report unavailable':managerReportSource)+'</div>';const summary=sourceNote+'<div class="grid" style="margin-top:10px"><div class="notice"><b>Parts Lists</b><div class="stat">'+new Set(rows.map(r=>r.listNo)).size+'</div></div><div class="notice"><b>Part Lines</b><div class="stat">'+rows.length+'</div></div><div class="notice"><b>Total Qty</b><div class="stat">'+qty+'</div></div><div class="notice"><b>Recorded Amount</b><div class="stat">'+cost.toFixed(3)+' OMR</div><span class="small">Uses stored commercial amounts without assuming unit vs line pricing.</span></div><div class="notice"><b>Fitted Qty</b><div class="stat">'+fittedQty+'</div></div><div class="notice"><b>Returned Qty</b><div class="stat">'+returnedQty+'</div></div><div class="notice"><b>Unavailable Qty</b><div class="stat">'+unavailableQty+'</div></div></div>';const st=Object.entries(statuses).map(([s,n])=>'<span class="pill">'+esc(s)+' '+n+'</span>').join('');const table=rows.length?'<div style="overflow:auto"><table><thead><tr><th>Activity Date</th><th>PL</th><th>JC</th><th>Vehicle</th><th>Part</th><th>Qty</th><th>Status</th><th>Supplier</th><th>Recorded Amount OMR</th></tr></thead><tbody>'+rows.map(r=>'<tr><td>'+esc(String(r.activityAt||r.createdAt||'').slice(0,10))+'</td><td>'+esc(r.listNo)+'</td><td>'+esc(r.jobCard)+'</td><td>'+esc(r.vehicle)+'</td><td>'+esc(r.name)+(r.partNo?'<div class="small">'+esc(r.partNo)+'</div>':'')+'</td><td>'+r.qty+'</td><td>'+esc(r.status)+'</td><td>'+esc(r.supplier)+'</td><td>'+r.amount.toFixed(3)+'</td></tr>').join('')+'</tbody></table></div>':'<p class="muted">No matching Spare Parts records.</p>';const body=document.getElementById('v2SpReportBody');if(body)body.innerHTML=summary+'<div style="margin:8px 0">'+st+'</div>'+table}
async function renderPurchaserDashboard(){const rows=await hydrateAuthoritativeLists(),items=rows.flatMap(l=>(l.items||[]).map(i=>Object.assign({listNo:l.listNo,jobCard:l.jobCard,vehicle:l.vehicle,registration:l.registration},i))),cards=[['🔎','New / Enquiry',items.filter(i=>['LISTED','ENQUIRY'].includes(i.status)).length,'LISTED'],['💰','Quotations',items.filter(i=>i.status==='QUOTED').length,'QUOTED'],['🛒','Orders / Waiting',items.filter(i=>i.status==='ORDERED').length,'ORDERED'],['📦','Received',items.filter(i=>i.status==='RECEIVED').length,'RECEIVED'],['↩️','Returns / Problems',items.filter(i=>['RETURNED','UNAVAILABLE'].includes(i.status)).length,'RETURNED'],['📚','Parts History',items.length,'ALL']];const recent=rows.slice().sort((a,b)=>new Date(b.updatedAt||b.createdAt||0)-new Date(a.updatedAt||a.createdAt||0)).slice(0,6).map(r=>'<button class="secondary" style="width:100%;text-align:left;margin:4px 0" onclick="zukaitV2.sparePartsMain.openList(\''+esc(r.listNo)+'\')"><b>'+esc(r.listNo)+'</b> · JC '+esc(r.jobCard)+'<small style="display:block">'+esc(r.vehicle||'')+' · '+((r.items||[]).length)+' part(s)</small></button>').join('')||'<p class="muted">No recent Parts Lists.</p>';window.openModal(withNav('<div class="v2-sp-purchaser"><h3 style="margin:0">🛒 Purchaser — Spare Parts</h3><small>Enquiry · Supplier · Quotation · Order · Receiving</small><div style="display:grid;grid-template-columns:1fr auto;gap:8px;margin-top:12px"><input id="v2SpManagerSearch" placeholder="Search JC / PL / Vehicle / Part"><button class="blue" onclick="zukaitV2.sparePartsMain.managerSearch()">Search</button></div><div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px;margin-top:12px">'+cards.map(x=>'<button class="secondary" style="min-height:92px;text-align:left;padding:12px" onclick="zukaitV2.sparePartsMain.managerBucket(\''+x[3]+'\')"><span style="font-size:22px">'+x[0]+'</span><b style="display:block;margin-top:5px">'+x[1]+'</b><strong style="font-size:20px">'+x[2]+'</strong></button>').join('')+'</div><h4 style="margin:14px 0 6px">Recent Activity</h4>'+recent+'</div>'))}
function managerDashboardSummary(rows=read()){const items=rows.flatMap(l=>(l.items||[]).map(i=>Object.assign({list:l},i))),problemStatuses=new Set(['RETURNED','UNAVAILABLE']),waitingStatuses=new Set(['ORDERED']),finalStatuses=new Set(['RECEIVED','SUPERVISOR_VERIFIED','DENTER_CHECKED','SUPERVISOR_CONFIRMED','FITTED','CUSTOMER_SETTLEMENT']);const attention=items.filter(i=>problemStatuses.has(i.status)||i.cashSettlementRequired).length,waiting=items.filter(i=>waitingStatuses.has(i.status)).length,deliveredPending=rows.filter(r=>r.vehicleDeliveredPartsPending===true||r.deliveryPartsPending===true||r.deliveredPendingParts===true||String(r.status||'').toUpperCase()==='DELIVERED_PENDING').length,purchaseCompleted=rows.filter(r=>(r.items||[]).length>0&&(r.items||[]).every(i=>finalStatuses.has(i.status))).length,now=new Date(),from=new Date(now.getFullYear(),now.getMonth(),1).getTime(),to=new Date(now.getFullYear(),now.getMonth()+1,1).getTime(),monthSpend=items.reduce((sum,i)=>{const t=Date.parse(i.updatedAt||i.receivedAt||i.createdAt||0);return t>=from&&t<to?sum+reportAmount(i):sum},0);return {attention,waiting,deliveredPending,purchaseCompleted,monthSpend:Math.round(monthSpend*1000)/1000,totalLists:rows.length}}
function managerCreatePanel(){if(role()!=='Manager')return;window.openModal(withNav('<div class="v2-sp-manager-create"><h3>📝 Create Parts List</h3><p class="muted">Select an existing Job Card. Vehicle details and the PL number are filled automatically.</p><div style="display:grid;grid-template-columns:1fr auto;gap:8px"><input id="v2SpareJob" placeholder="Job Card number"><button onclick="zukaitV2.sparePartsMain.createFromUI()" style="'+spareBtn('primary')+'">CREATE PARTS LIST</button></div></div>','zukaitV2.sparePartsMain.open()'))}
function managerListMatches(r,bucket){const items=r.items||[],statuses=items.map(i=>String(i.status||'LISTED'));if(bucket==='ALL')return true;if(bucket==='NEW')return statuses.some(s=>['LISTED','ENQUIRY','QUOTED'].includes(s));if(bucket==='WAITING')return statuses.some(s=>s==='ORDERED');if(bucket==='ATTENTION')return items.some(i=>['RETURNED','UNAVAILABLE'].includes(i.status)||i.cashSettlementRequired);if(bucket==='PURCHASE_COMPLETED')return items.length>0&&items.every(i=>['RECEIVED','SUPERVISOR_VERIFIED','DENTER_CHECKED','SUPERVISOR_CONFIRMED','FITTED','CUSTOMER_SETTLEMENT'].includes(i.status));if(bucket==='DELIVERED_PENDING')return r.vehicleDeliveredPartsPending===true||r.deliveryPartsPending===true||r.deliveredPendingParts===true||String(r.status||'').toUpperCase()==='DELIVERED_PENDING';return statuses.some(s=>bucket==='RECEIVED'?['RECEIVED','SUPERVISOR_VERIFIED'].includes(s):s===bucket)}
async function renderManagerDashboard(){const rows=await hydrateAuthoritativeLists(),sum=managerDashboardSummary(rows),cards=[['📝','Create Parts List','New PL','CREATE'],['🆕','New Parts List',rows.filter(r=>managerListMatches(r,'NEW')).length,'NEW'],['📋','Parts List',rows.length,'ALL'],['⏳','Parts Waiting',sum.waiting,'WAITING'],['✅','Purchase Completed',sum.purchaseCompleted,'PURCHASE_COMPLETED'],['🚗','Delivered Vehicle – Pending Parts',sum.deliveredPending,'DELIVERED_PENDING']];const recent=rows.slice().sort((a,b)=>new Date(b.updatedAt||b.createdAt||0)-new Date(a.updatedAt||a.createdAt||0)).slice(0,6).map(r=>'<button class="secondary" style="width:100%;text-align:left;margin:4px 0" onclick="zukaitV2.sparePartsMain.openList(\''+esc(r.listNo)+'\')"><b>'+esc(r.listNo)+'</b> · JC '+esc(r.jobCard)+'<small style="display:block">'+esc([r.vehicle,r.model,r.year,r.registration].filter(Boolean).join(' · '))+' · '+((r.items||[]).length)+' part(s)</small></button>').join('')||'<p class="muted">No recent Parts Lists.</p>';const summary='<div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:12px"><div class="notice"><b>⚠ Parts Attention</b><div class="stat">'+sum.attention+'</div></div><div class="notice"><b>⏳ Parts Waiting</b><div class="stat">'+sum.waiting+'</div></div><div class="notice"><b>🚗 Delivered – Pending</b><div class="stat">'+sum.deliveredPending+'</div></div><div class="notice"><b>💰 Month Purchase</b><div class="stat">'+sum.monthSpend.toFixed(3)+' OMR</div></div></div>';const operational=cards.map(x=>{const kind=x[3]==='CREATE'?'primary':x[3]==='WAITING'?'wait':x[3]==='PURCHASE_COMPLETED'?'success':x[3]==='DELIVERED_PENDING'?'danger':x[3]==='NEW'?'info':'neutral';return '<button style="'+spareBtn(kind)+';min-height:96px;text-align:left;padding:12px;border-radius:15px" onclick="'+(x[3]==='CREATE'?'zukaitV2.sparePartsMain.managerCreatePanel()':"zukaitV2.sparePartsMain.managerBucket('"+x[3]+"')")+'"><span style="font-size:22px">'+x[0]+'</span><b style="display:block;margin-top:5px">'+x[1]+'</b><strong style="font-size:19px">'+x[2]+'</strong></button>'}).join('');window.openModal(withNav('<div class="v2-sp-manager"><div><h3 style="margin:0">🚗⚙️ Spare Parts</h3><small>Manager · Supervisor operational view + management controls</small></div>'+summary+'<div style="display:grid;grid-template-columns:1fr auto;gap:8px;margin-top:12px"><input id="v2SpManagerSearch" placeholder="Search JC / PL / registration / vehicle / part"><button class="blue" onclick="zukaitV2.sparePartsMain.managerSearch()">Search</button></div><h4 style="margin:14px 0 7px">Operations</h4><div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px">'+operational+'</div><h4 style="margin:14px 0 7px">Manager Controls</h4><div style="display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px"><button class="blue" style="min-height:74px" onclick="zukaitV2.sparePartsMain.openManagerReport()">📊 Reports</button><button class="secondary" style="min-height:74px" onclick="zukaitV2.sparePartsMain.managerBucket(\'ALL\')">✏️ View / Edit</button><button class="secondary" style="min-height:74px" onclick="zukaitV2.sparePartsMain.managerBucket(\'ATTENTION\')">⚠ Attention '+sum.attention+'</button><button class="secondary" style="min-height:74px" onclick="zukaitV2.sparePartsMain.managerBucket(\'ALL\')">📚 History / Audit</button></div><h4 style="margin:14px 0 6px">Recent Activity</h4>'+recent+'</div>'))}
function managerBucket(status){const rows=read(),filtered=rows.filter(r=>managerListMatches(r,status));const body=filtered.length?filtered.slice(0,200).map(r=>'<button class="secondary" style="width:100%;margin:5px 0;text-align:left" onclick="zukaitV2.sparePartsMain.openList(\''+esc(r.listNo)+'\')"><b>'+esc(r.listNo)+'</b> · JC '+esc(r.jobCard)+'<small style="display:block">'+esc([r.vehicle,r.model,r.year,r.registration].filter(Boolean).join(' · '))+' · '+(r.items||[]).length+' part(s)</small></button>').join(''):'<p class="muted">No matching Parts Lists.</p>';window.openModal(withNav('<h3>Spare Parts</h3>'+body,'zukaitV2.sparePartsMain.open()'))}
function managerSearch(){const q=String(document.getElementById('v2SpManagerSearch')?.value||'').trim().toLowerCase();if(!q)return managerBucket('ALL');const rows=read().filter(r=>[r.listNo,r.jobCard,r.vehicle,r.registration].some(v=>String(v||'').toLowerCase().includes(q))||(r.items||[]).some(i=>[i.name,i.partNo,i.supplier].some(v=>String(v||'').toLowerCase().includes(q))));if(rows.length===1)return openList(rows[0].listNo);const body=rows.length?rows.map(r=>'<button class="secondary" style="width:100%;margin:5px 0;text-align:left" onclick="zukaitV2.sparePartsMain.openList(\''+esc(r.listNo)+'\')"><b>'+esc(r.listNo)+'</b> · JC '+esc(r.jobCard)+'</button>').join(''):'<p class="muted">No matching Parts Lists.</p>';window.openModal(withNav('<h3>Search Results</h3>'+body))}
async function render(){if(!canOpen()){alert('Spare Parts access unavailable.');return}if(role()==='Manager')return renderManagerDashboard();if(role()==='Purchaser')return renderPurchaserDashboard();const allRows=await hydrateAuthoritativeLists(),rows=denterView()?allRows.filter(r=>{const j=job(r.jobCard);const uid=String(currentUser()?.id||'');return (Array.isArray(j?.assignedEmployees)&&j.assignedEmployees.map(String).includes(uid))||assignedToJob(r.jobCard,uid)}):allRows;const body=rows.length?rows.slice(0,100).map(r=>'<button class="secondary" style="width:100%;margin:5px 0;text-align:left" onclick="zukaitV2.sparePartsMain.openList(\''+esc(r.listNo)+'\')"><b>'+esc(r.listNo)+'</b> · JC '+esc(r.jobCard)+' · '+r.items.length+' part(s)</button>').join(''):'<p class="muted">No Parts Lists yet.</p>';const createBox=denterView()?'':'<p class="muted">Create a Parts List from an existing Job Card. List numbers are automatic.</p><div style="display:grid;grid-template-columns:1fr auto;gap:8px"><input id="v2SpareJob" placeholder="Job Card number"><button class="blue" onclick="zukaitV2.sparePartsMain.createFromUI()">Create Parts List</button></div><hr>';const report=role()==='Manager'?'<button class="blue" style="width:100%;margin:8px 0" onclick="zukaitV2.sparePartsMain.openManagerReport()">📊 Spare Parts Report</button>':'';const html='<h3>🔩 Spare Parts</h3>'+report+createBox+body;if(typeof window.openModal==='function')window.openModal(withNav(html))}
async function createFromUI(){if(!canManage()){alert('FORBIDDEN');return}const jobCard=normalizeJobCard(document.getElementById('v2SpareJob')?.value);if(!jobCard){alert('JOB_CARD_REQUIRED');return}if(!job(jobCard)){alert('JOB_CARD_NOT_FOUND');return}if(!navigator.onLine||!window.zukaitCloud?.allocateSparePartList){alert('Parts List creation requires an online server allocation to prevent duplicate PL numbers.');return}let allocation;try{allocation=await window.zukaitCloud.allocateSparePartList(jobCard)}catch(e){alert(String(e?.code||e?.message||'PL_ALLOCATION_FAILED'));return}const rows=read();let list=rows.find(x=>x.listNo===allocation.list_no);if(!list){const j=job(jobCard)||{},u=currentUser()||{};list={listNo:allocation.list_no,jobCard,vehicle:j.vehicle||j.make||'',model:j.model||'',year:j.year||'',registration:j.reg||j.registration||'',status:allocation.status||'OPEN',items:[],createdAt:allocation.created_at||new Date().toISOString(),createdBy:allocation.created_by||u.id||null,createdByRole:u.role||null,serverAllocated:true};rows.unshift(list);write(rows)}const r={ok:true,list};const synced=await syncList(r.list);if(!synced.ok&&synced.reason!=='V2_TRANSPORT_UNAVAILABLE')console.warn('Spare Parts sync deferred',synced.reason);openList(r.list.listNo)}
function partsListText(r){const lines=['ZUKAIT INTERNATIONAL LLC','SPARE PARTS LIST '+r.listNo,'JC '+r.jobCard,[r.vehicle,r.model,r.year,r.registration].filter(Boolean).join(' · '),''];(r.items||[]).forEach((x,i)=>lines.push((i+1)+'. '+x.name+(x.partNo?' · '+x.partNo:'')+' · Qty '+x.qty+' · '+(x.status||'LISTED')+(x.supplier?' · '+x.supplier:'')+(reportAmount(x)?' · '+reportAmount(x).toFixed(3)+' OMR':'')));return lines.join('\n')}
function printPartsList(no){if(role()!=='Manager')return;const r=read().find(x=>x.listNo===no);if(!r)return;const rows=(r.items||[]).map((x,i)=>'<tr><td>'+(i+1)+'</td><td>'+esc(x.name)+'</td><td>'+esc(x.partNo||'')+'</td><td>'+Number(x.qty||0)+'</td><td>'+esc(x.status||'LISTED')+'</td><td>'+esc(x.supplier||'')+'</td><td>'+reportAmount(x).toFixed(3)+'</td></tr>').join('');const w=window.open('','_blank');if(!w){alert('Print window blocked.');return}w.document.write('<!doctype html><html><head><title>'+esc(no)+'</title><style>body{font-family:Arial;padding:20px;color:#111}table{width:100%;border-collapse:collapse}th,td{border:1px solid #bbb;padding:7px;text-align:left}h2,h3{margin:4px 0}</style></head><body><h2>Zukait International LLC</h2><h3>'+esc(r.listNo)+' · JC '+esc(r.jobCard)+'</h3><p>'+esc([r.vehicle,r.model,r.year,r.registration].filter(Boolean).join(' · '))+'</p><table><tr><th>#</th><th>Part</th><th>Part No.</th><th>Qty</th><th>Status</th><th>Supplier</th><th>Amount OMR</th></tr>'+rows+'</table></body></html>');w.document.close();w.focus();w.print()}
async function sharePartsList(no){if(role()!=='Manager')return;const r=read().find(x=>x.listNo===no);if(!r)return;const text=partsListText(r);try{if(navigator.share){await navigator.share({title:'Spare Parts '+r.listNo,text});return}}catch(e){if(e?.name==='AbortError')return}try{await navigator.clipboard.writeText(text);alert('Parts List copied. You can share it from this device.')}catch(_){alert('Share is unavailable on this device. Use Print / PDF.')}}
function whatsAppPartsList(no){if(role()!=='Manager')return;const r=read().find(x=>x.listNo===no);if(!r)return;const url='https://wa.me/?text='+encodeURIComponent(partsListText(r));try{const w=window.open(url,'_blank');if(w)return}catch(_){}window.location.href=url}
function openManagerEditItem(listNo,itemId){if(role()!=='Manager')return;const list=read().find(x=>x.listNo===listNo),item=list?.items?.find(x=>x.id===itemId);if(!item)return;const amount=reportAmount(item);window.openModal(withNav('<h3>✏️ Edit Part</h3><p><b>'+esc(listNo)+'</b> · JC '+esc(list.jobCard)+'</p><label>Part Name<input id="v2SpEditName" value="'+esc(item.name||'')+'"></label><label>Part No.<input id="v2SpEditPartNo" value="'+esc(item.partNo||'')+'"></label><label>Quantity<input id="v2SpEditQty" type="number" min="1" value="'+Number(item.qty||1)+'"></label><label>Supplier<input id="v2SpEditSupplier" value="'+esc(item.supplier||'')+'"></label><label>Recorded Amount OMR<input id="v2SpEditAmount" type="number" min="0" step="0.001" value="'+(amount||'')+'"></label><button class="big-action" onclick="zukaitV2.sparePartsMain.saveManagerItemEdit(\''+esc(listNo)+'\',\''+esc(itemId)+'\')" style="'+spareBtn('success')+'">SAVE CHANGES</button>','zukaitV2.sparePartsMain.openList(\''+esc(listNo)+'\')'))}
async function saveManagerItemEdit(listNo,itemId){if(role()!=='Manager')return;const rows=read(),list=rows.find(x=>x.listNo===listNo),item=list?.items?.find(x=>x.id===itemId);if(!item)return;const name=String(document.getElementById('v2SpEditName')?.value||'').trim(),partNo=String(document.getElementById('v2SpEditPartNo')?.value||'').trim().toUpperCase(),qty=validQty(document.getElementById('v2SpEditQty')?.value),supplier=String(document.getElementById('v2SpEditSupplier')?.value||'').trim(),amountRaw=document.getElementById('v2SpEditAmount')?.value,amount=amountRaw===''?null:validMoney(amountRaw);if(!name)return alert('Part name is required.');if(qty==null)return alert('Enter a valid quantity.');if(amountRaw!==''&&amount==null)return alert('Enter a valid amount.');const before={name:item.name,partNo:item.partNo,qty:item.qty,supplier:item.supplier,purchaseAmount:item.purchaseAmount};Object.assign(item,{name,partNo,qty,supplier,updatedAt:new Date().toISOString(),updatedBy:currentUser()?.id||null});if(amount==null)delete item.purchaseAmount;else item.purchaseAmount=amount;list.updatedAt=item.updatedAt;write(rows);const synced=await commitEvent({eventId:'spare-edit-'+itemId+'-'+Date.now(),entityId:itemId,actorId:currentUser()?.id||null,type:'SPARE_PART_ITEM_EDITED',revision:Number(item.revision||1)+1,payload:{partId:itemId,listNo,jobCard:list.jobCard,before,after:{name,partNo,qty,supplier,purchaseAmount:amount}}});if(!synced.ok&&!synced.queued){Object.assign(item,before);write(rows);alert('Unable to save change: '+synced.reason);return}openList(listNo)}
function purchaserActions(listNo,item){if(role()!=='Purchaser')return '';const next={LISTED:'ENQUIRY',ENQUIRY:'QUOTED',QUOTED:'ORDERED',ORDERED:'RECEIVED'}[item.status];return '<div style="margin-top:8px"><button class="blue" onclick="zukaitV2.sparePartsMain.openPurchaserItem(\''+esc(listNo)+'\',\''+esc(item.id)+'\')">🛒 Purchasing Details</button>'+(next?'<button class="secondary" style="margin-left:6px" onclick="zukaitV2.sparePartsMain.purchaserAdvance(\''+esc(listNo)+'\',\''+esc(item.id)+'\',\''+next+'\')">'+esc(next.replaceAll('_',' '))+'</button>':'')+'</div>'}
function openPurchaserItem(listNo,itemId){if(role()!=='Purchaser')return;const list=read().find(x=>x.listNo===listNo),item=list?.items?.find(x=>x.id===itemId);if(!item)return;window.openModal(withNav('<h3>🛒 Purchasing Details</h3><p><b>'+esc(listNo)+'</b> · JC '+esc(list.jobCard)+'</p><p>'+esc(item.name)+' · Qty '+Number(item.qty||0)+' · '+esc(item.status)+'</p><label>Supplier<input id="v2SpPurchaseSupplier" value="'+esc(item.supplier||'')+'"></label><label>Quotation OMR<input id="v2SpPurchaseQuote" type="number" min="0" step="0.001" value="'+(item.quoteAmount??'')+'"></label><label>Purchase Amount OMR<input id="v2SpPurchaseAmount" type="number" min="0" step="0.001" value="'+(item.purchaseAmount??'')+'"></label><button class="blue big-action" onclick="zukaitV2.sparePartsMain.savePurchaserItem(\''+esc(listNo)+'\',\''+esc(itemId)+'\')">SAVE PURCHASING DETAILS</button>','zukaitV2.sparePartsMain.openList(\''+esc(listNo)+'\')'))}
async function savePurchaserItem(listNo,itemId){if(role()!=='Purchaser')return;const patch={supplier:document.getElementById('v2SpPurchaseSupplier')?.value||'',quoteAmount:document.getElementById('v2SpPurchaseQuote')?.value||'',purchaseAmount:document.getElementById('v2SpPurchaseAmount')?.value||''},r=updateCommercial(listNo,itemId,patch);if(!r.ok)return alert(r.reason);const list=read().find(x=>x.listNo===listNo);const synced=await commitEvent({eventId:'spare-commercial-'+itemId+'-'+Date.now(),entityId:itemId,actorId:currentUser()?.id||null,type:'SPARE_PART_COMMERCIAL_UPDATED',revision:Number(r.item.revision||1)+1,payload:{partId:itemId,listNo,jobCard:list?.jobCard,supplier:r.item.supplier||'',quoteAmount:r.item.quoteAmount??null,purchaseAmount:r.item.purchaseAmount??null}});if(!synced.ok&&!synced.queued)return alert('Unable to sync purchasing details: '+synced.reason);openList(listNo)}
async function purchaserAdvance(listNo,itemId,to){if(role()!=='Purchaser')return;const r=await transitionItem(listNo,itemId,to);if(!r.ok)return alert(r.reason);openList(listNo)}
function openList(no){const r=read().find(x=>x.listNo===no);if(!r)return;const isManager=role()==='Manager';const items=r.items.map((x,i)=>'<div class="card"><div style="display:flex;justify-content:space-between;gap:8px;align-items:flex-start"><div><b>'+(i+1)+'. '+esc(x.name)+'</b><div class="small">'+esc(x.partNo||'')+' · Qty '+x.qty+' · <span style="display:inline-block;padding:3px 8px;border-radius:999px;color:white;background:'+statusColour(x.status)+'">'+esc(x.status)+'</span></div>'+(isManager&&x.supplier?'<div class="small">Supplier: '+esc(x.supplier)+'</div>':'')+(isManager&&reportAmount(x)?'<div class="small">Amount: '+reportAmount(x).toFixed(3)+' OMR</div>':'')+'</div>'+(isManager?'<button onclick="zukaitV2.sparePartsMain.openManagerEditItem(\''+esc(no)+'\',\''+esc(x.id)+'\')" style="'+spareBtn('edit')+'">Edit</button>':'')+'</div>' +purchaserActions(no,x)+(denterView()?'<button style="'+spareBtn('wait')+';margin-top:6px" onclick="zukaitV2.sparePartsMain.notifySupervisor(\''+esc(no)+'\',\''+esc(x.id)+'\')">Notify Supervisor</button>':'')+'</div>').join('')||'<p class="muted">No parts added.</p>';const add=(denterView()||role()==='Purchaser')?'':'<hr><div style="display:grid;grid-template-columns:1fr 110px auto;gap:6px"><input id="v2PartName" placeholder="Part name"><input id="v2PartQty" type="number" min="1" value="1"><button onclick="zukaitV2.sparePartsMain.addFromUI(\''+esc(no)+'\')" style="'+spareBtn('primary')+'">Add</button></div>';const managerActions=isManager?'<div style="display:flex;gap:7px;flex-wrap:wrap;margin:10px 0"><button onclick="zukaitV2.sparePartsMain.printPartsList(\''+esc(no)+'\')" style="'+spareBtn('primary')+'">🖨 Print / PDF</button><button onclick="zukaitV2.sparePartsMain.sharePartsList(\''+esc(no)+'\')" style="'+spareBtn('info')+'">↗ Share</button><button onclick="zukaitV2.sparePartsMain.whatsAppPartsList(\''+esc(no)+'\')" style="'+spareBtn('success')+'">WhatsApp</button></div>':'';window.openModal(withNav('<h3>'+esc(r.listNo)+' · JC '+esc(r.jobCard)+'</h3><p>'+esc([r.vehicle,r.model,r.year,r.registration].filter(Boolean).join(' · '))+'</p>'+managerActions+items+add,'zukaitV2.sparePartsMain.open()'))}
async function notifySupervisor(listNo,itemId){const list=read().find(x=>x.listNo===listNo),item=list?.items?.find(x=>x.id===itemId),u=currentUser()||{},wf=window.zukaitV2?.spareParts;if(!item||!wf)return {ok:false,reason:'PART_NOT_FOUND'};const n=wf.notifySupervisor(Object.assign({},item,{jobCard:list.jobCard}),{role:u.role,actorId:u.id});if(!n.ok)return n;const synced=await commitEvent({eventId:'spare-notice-'+itemId+'-'+Date.now(),entityId:itemId,actorId:u.id||null,type:'SPARE_PART_DENTER_NOTICE',revision:Number(item.revision||1),payload:{partId:itemId,listNo,jobCard:list.jobCard,targetRole:'Supervisor',status:item.status}});if(!synced.ok&&synced.reason!=='V2_TRANSPORT_UNAVAILABLE')return {ok:false,reason:'SYNC_FAILED',detail:synced.reason};alert('Supervisor notified.');return {ok:true,notification:n.notification,sync:synced}}
async function transitionItem(listNo,itemId,to,reason=''){const rows=read(),list=rows.find(x=>x.listNo===listNo);if(!list)return {ok:false,reason:'LIST_NOT_FOUND'};const idx=list.items.findIndex(x=>x.id===itemId);if(idx<0)return {ok:false,reason:'PART_NOT_FOUND'};const u=currentUser()||{},wf=window.zukaitV2?.spareParts;if(!wf)return {ok:false,reason:'WORKFLOW_UNAVAILABLE'};const moved=wf.transition(Object.assign({},list.items[idx],{jobCard:list.jobCard}),to,{role:u.role,actorId:u.id,deviceId:window.zukaitDeviceId||null,reason});if(!moved.ok)return moved;const previous=list.items[idx];list.items[idx]=moved.item;list.updatedAt=moved.item.updatedAt;write(rows);const synced=await commitEvent({eventId:'spare-status-'+itemId+'-'+to+'-'+Date.now(),entityId:itemId,actorId:u.id||null,type:'SPARE_PART_STATUS_CHANGED',revision:Number(previous.revision||1)+1,payload:{partId:itemId,listNo,jobCard:list.jobCard,from:previous.status,to,reason:reason||null}});if(!synced.ok&&synced.reason!=='V2_TRANSPORT_UNAVAILABLE'){list.items[idx]=previous;write(rows);return {ok:false,reason:'SYNC_FAILED',detail:synced.reason}}return {ok:true,item:moved.item,sync:synced}}
async function addFromUI(no){const r=addItem(no,{name:document.getElementById('v2PartName')?.value,qty:document.getElementById('v2PartQty')?.value});if(!r.ok){if(r.reason==='OPEN_LIST_EXISTS'&&r.list){openList(r.list.listNo);return}alert(r.reason);return}const item=r.list.items[r.list.items.length-1],u=currentUser()||{};const synced=await commitEvent({eventId:'spare-item-'+item.id,entityId:item.id,actorId:u.id||null,type:'SPARE_PART_LISTED',revision:1,payload:{partId:item.id,listNo:no,jobCard:r.list.jobCard,name:item.name,partNo:item.partNo,qty:item.qty}});if(!synced.ok&&synced.reason!=='V2_TRANSPORT_UNAVAILABLE')console.warn('Spare part item sync deferred',synced.reason);openList(no)}
async function syncList(list){const u=currentUser()||{};return commitEvent({eventId:'spare-list-'+list.listNo+'-'+Date.now(),entityId:list.listNo,actorId:u.id||null,type:'SPARE_PART_LIST_CREATED',revision:1,payload:{jobCard:list.jobCard,partId:list.listNo,listNo:list.listNo,vehicle:list.vehicle,registration:list.registration}})}
window.openSpareParts=render;window.openSparePartsModule=render;window.zukaitV2=Object.assign(window.zukaitV2||{},{sparePartsMain:{open:render,create,addItem,canManage,canPurchase,openList,openForJobCard,partsSummary,listsForJobCard,createFromUI,addFromUI,transitionItem,notifySupervisor,updateCommercial,validQty,validMoney,commitEvent,syncList,openManagerReport,renderManagerReport,reportRows,printManagerReport,shareManagerReport,whatsAppManagerReport,whatsAppReportText,managerReportText,managerReportFilters,filteredReportRows,reportActivityAt,loadAuthoritativeManagerReport,normalizeReportRow,hydrateAuthoritativeLists,hydrateFromServerRows,renderManagerDashboard,renderPurchaserDashboard,managerDashboardSummary,managerCreatePanel,managerBucket,managerSearch,printPartsList,sharePartsList,whatsAppPartsList,openManagerEditItem,saveManagerItemEdit,purchaserActions,openPurchaserItem,savePurchaserItem,purchaserAdvance}});
})();
