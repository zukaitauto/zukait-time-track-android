import fs from'node:fs';import assert from'node:assert/strict';import vm from'node:vm';const s={window:{},Date};vm.createContext(s);for(const f of['contract.js','lists.js','quantity.js','completion.js'])vm.runInContext(fs.readFileSync('app/src/main/assets/v2/features/spare-parts/'+f,'utf8'),s);const p=s.window.zukaitV2.spareParts;const lists=[];for(let i=1;i<=2500;i++)lists.push({number:'PL-'+String(i).padStart(4,'0'),jobCard:'JC'+i,registration:'R'+i,make:i%2?'Toyota':'Lexus',model:'M'+i,lines:[{id:'P'+i,name:'Part '+i,qty:1,status:'ORDERED'}]});const t=Date.now();const found=p.lists.search(lists,'JC2499');assert.equal(found.length,1);assert.equal(found[0].jobCard,'JC2499');for(const x of lists.slice(0,200))assert.equal(p.completion.evaluate(x).complete,false);const ms=Date.now()-t;assert.ok(ms<1500,'scale operations too slow: '+ms+'ms');console.log('Spare Parts 2500-list scale check: '+ms+'ms');