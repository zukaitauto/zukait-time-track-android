import fs from'node:fs';import assert from'node:assert/strict';import vm from'node:vm';const s={window:{}};vm.createContext(s);for(const f of['contract.js','calendar.js','completion.js','lists.js'])vm.runInContext(fs.readFileSync('app/src/main/assets/v2/features/spare-parts/'+f,'utf8'),s);const p=s.window.zukaitV2.spareParts,base={number:'PL001',jobCard:'JC1',createdAt:'2026-09-23T08:00:00+04:00',view:'NEW',lines:[{id:'P1',name:'Bumper',qty:1,status:'LISTED'}]};let x=p.lists.refreshView(base,{now:'2026-09-24T18:00:00+04:00'});assert.equal(x.view,'NEW');x=p.lists.refreshView(base,{now:'2026-09-27T18:00:00+04:00'});assert.notEqual(x.view,'NEW');assert.ok(x.workingAgeDays>=3);const added=Object.assign({},x,{additionalUnread:true});assert.equal(p.lists.refreshView(added,{now:'2026-09-27T18:00:00+04:00'}).view,'NEW');const viewed=p.lists.markViewed(added,{role:'Purchaser',serverTime:'2026-09-27T18:01:00+04:00'});assert.equal(viewed.ok,true);assert.notEqual(p.lists.refreshView(viewed.list,{now:'2026-09-27T18:02:00+04:00'}).view,'NEW');console.log('Spare Parts New-period automation: ok');