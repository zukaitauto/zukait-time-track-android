import fs from'node:fs';import assert from'node:assert/strict';import vm from'node:vm';const s={window:{}};vm.createContext(s);for(const f of['contract.js','analytics.js','reports.js'])vm.runInContext(fs.readFileSync('app/src/main/assets/v2/features/spare-parts/'+f,'utf8'),s);const p=s.window.zukaitV2.spareParts;let r=p.partsReports.presetRange('TODAY','2026-09-25T00:30:00+04:00');assert.equal(new Date(r.from).toISOString(),'2026-09-24T20:00:00.000Z');assert.equal(new Date(r.to).toISOString(),'2026-09-25T20:00:00.000Z');r=p.partsReports.presetRange('MONTH','2026-09-25T23:00:00+04:00');assert.equal(new Date(r.from).toISOString(),'2026-08-31T20:00:00.000Z');assert.equal(new Date(r.to).toISOString(),'2026-09-30T20:00:00.000Z');const lists=[{number:'PL001',jobCard:'JC1',lines:[{name:'A',qty:1,status:'CONFIRMED',finalPriceOMR:5,finalPurchaseAt:'2026-09-24T21:00:00Z'}]}];assert.equal(p.partsReports.preset(lists,'TODAY','2026-09-25T10:00:00+04:00').total,5);console.log('Spare Parts Oman-local reports: ok');