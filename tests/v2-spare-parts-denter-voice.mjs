import fs from'node:fs';import assert from'node:assert/strict';import vm from'node:vm';const s={window:{}};vm.createContext(s);for(const f of['contract.js','completion.js','standalone-controller.js'])vm.runInContext(fs.readFileSync('app/src/main/assets/v2/features/spare-parts/'+f,'utf8'),s);const p=s.window.zukaitV2.spareParts,lists=[{number:'PL001',jobCard:'JC55',lines:[{id:'P1',name:'Bumper',qty:1,status:'RECEIVED'}]}];let st=p.controller.createStore({role:'Denter',lists}),r=st.dispatch('DENTER_REPORT',{pl:'PL001',type:'RECEIVED_PART_PROBLEM',partId:'P1',voiceNote:{id:'VN1',durationSec:8,mimeType:'audio/webm',transcript:'Mounting damaged'},ctx:{role:'Denter',actorId:'EMP4'}});assert.equal(r.ok,true);let x=st.findPL('PL001'),dr=x.denterReports[0];assert.equal(dr.note,'');assert.equal(dr.voiceNote.id,'VN1');assert.equal(dr.voiceNote.durationSec,8);assert.equal(dr.voiceNote.transcript,'Mounting damaged');r=st.dispatch('DENTER_REPORT',{pl:'PL001',type:'URGENT_PART',ctx:{role:'Denter'}});assert.equal(r.ok,false);assert.equal(r.code,'REPORT_CONTENT_REQUIRED');console.log('Spare Parts Denter voice reporting: ok');