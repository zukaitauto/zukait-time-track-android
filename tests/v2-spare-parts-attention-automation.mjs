import fs from'node:fs';import assert from'node:assert/strict';import vm from'node:vm';const s={window:{}};vm.createContext(s);for(const f of['contract.js','analytics.js','attention.js'])vm.runInContext(fs.readFileSync('app/src/main/assets/v2/features/spare-parts/'+f,'utf8'),s);const p=s.window.zukaitV2.spareParts,lists=[{number:'PL001',jobCard:'JC1',view:'NEW',purchaseCapOMR:10,lines:[{id:'a',status:'LISTED',finalPriceOMR:null},{id:'b',status:'RECEIVED',finalPriceOMR:null},{id:'c',status:'SUPERVISOR_CONFIRMED',finalPriceOMR:12}]},{number:'PL002',jobCard:'JC2',deliveredPending:true,lines:[{id:'d',status:'ORDERED'}]}];let a=p.attention.counts(lists,'Purchaser');assert.ok(a.total>=3);assert.equal(a.byCode.NEW_LIST_NOT_VIEWED,1);assert.equal(a.byCode.AWAITING_SUPERVISOR_CHECK,undefined);assert.equal(a.byCode.ORDERED_NOT_ARRIVED,1);lists[0].denterReports=[{id:'DR1',type:'URGENT_PART',status:'OPEN',partId:'a'}];a=p.attention.counts(lists,'Supervisor');assert.equal(a.byCode.AWAITING_SUPERVISOR_CHECK,1);assert.equal(a.byCode.PRICE_WAITING,undefined);assert.equal(a.byCode.DENTER_URGENT_PART,1);a=p.attention.counts(lists,'Manager');assert.equal(a.byCode.PURCHASE_CAP_EXCEEDED,1);assert.ok(a.items.every(x=>x.pl));console.log('Spare Parts attention automation: ok');